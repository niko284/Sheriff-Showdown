--!strict
--!native
--!optimize 2
--!nolint LocalShadow
--!nolint LocalUnused
--!nolint FunctionUnused
--#selene: allow(shadowing)
-- File generated by Blink v0.18.8 (https://github.com/1Axen/Blink)
-- This file is not meant to be edited

_G._BLINK = _G._BLINK or {}
if _G._BLINK["BLINK"] then
	error("[Blink]: An instance of blink is already running with the remote scope \"BLINK\". Change the remote scope of either instance to avoid conflicts.")
end
_G._BLINK["BLINK"] = true
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local SerdesFunctions = {}
local BASE_EVENT_NAME = "BLINK"
local Invocations = 0

local SendSize = 64
local SendOffset = 0
local SendCursor = 0
local SendBuffer = buffer.create(64)
local SendInstances = {}

local RecieveCursor = 0
local RecieveBuffer = buffer.create(64)

local RecieveInstances = {}
local RecieveInstanceCursor = 0

local Null = newproxy()

type Entry = {
    value: any,
    next: Entry?
}

type Queue = {
    head: Entry?,
    tail: Entry?
}

type BufferSave = {
    Size: number, 
    Cursor: number, 
    Buffer: buffer, 
    Instances: {Instance}
}

local function Read(Bytes: number)
    local Offset = RecieveCursor
    RecieveCursor += Bytes
    return Offset
end

local function Save(): BufferSave
    return {
        Size = SendSize,
        Cursor = SendCursor,
        Buffer = SendBuffer,
        Instances = SendInstances
    }
end

local function Load(Save: BufferSave?)
    if Save then
        SendSize = Save.Size
        SendCursor = Save.Cursor
        SendOffset = Save.Cursor
        SendBuffer = Save.Buffer
        SendInstances = Save.Instances
        return
    end

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    SendInstances = {}
end

local function Invoke()
    if Invocations == 255 then
        Invocations = 0
    end

    local Invocation = Invocations
    Invocations += 1
    return Invocation
end

local function Allocate(Bytes: number)
    local InUse = (SendCursor + Bytes)
    if InUse > SendSize then
        --> Avoid resizing the buffer for every write
        while InUse > SendSize do
            SendSize *= 1.5
        end

        local Buffer = buffer.create(SendSize)
        buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
        SendBuffer = Buffer
    end

    SendOffset = SendCursor
    SendCursor += Bytes
    
    return SendOffset
end

local function CreateQueue(): Queue
    return {
        head = nil,
        tail = nil
    }
end

local function Pop(queue: Queue): any
    local head = queue.head
    if head == nil then
        return
    end

    queue.head = head.next
    return head.value
end

local function Push(queue: Queue, value: any)
    local entry: Entry = {
        value = value,
        next = nil
    }

    if queue.tail ~= nil then
        queue.tail.next = entry
    end

    queue.tail = entry

    if queue.head == nil then
        queue.head = entry
    end
end

local Calls = table.create(256)

local Events: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local Queue: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local SerdesEvent = "?"
local SerdesThread: thread?
Events.Reliable[0] = {}
Queue.Reliable[0] = table.create(256)
Events.Unreliable[0] = {}
Queue.Unreliable[0] = table.create(256)
Queue.Reliable[2] = table.create(256)
Queue.Reliable[4] = table.create(256)
Queue.Reliable[5] = table.create(256)
Queue.Reliable[6] = table.create(256)
Events.Reliable[7] = {}
Queue.Reliable[7] = table.create(256)
Queue.Reliable[8] = table.create(256)
Queue.Reliable[9] = table.create(256)
Queue.Reliable[10] = table.create(256)
Events.Reliable[11] = {}
Queue.Reliable[11] = table.create(256)
Events.Reliable[12] = {}
Queue.Reliable[12] = table.create(256)
Events.Reliable[13] = {}
Queue.Reliable[13] = table.create(256)
Events.Reliable[14] = {}
Queue.Reliable[14] = table.create(256)
Queue.Reliable[15] = table.create(256)
Queue.Reliable[16] = table.create(256)
Queue.Reliable[17] = table.create(256)
Queue.Reliable[18] = table.create(256)
Queue.Reliable[19] = table.create(256)
Queue.Reliable[20] = table.create(256)
Queue.Reliable[21] = table.create(256)
Events.Reliable[22] = {}
Queue.Reliable[22] = table.create(256)
Queue.Reliable[23] = table.create(256)
Queue.Reliable[24] = table.create(256)
Events.Reliable[25] = {}
Queue.Reliable[25] = table.create(256)
Events.Reliable[26] = {}
Queue.Reliable[26] = table.create(256)
Queue.Reliable[27] = table.create(256)
Events.Reliable[28] = {}
Queue.Reliable[28] = table.create(256)
Events.Reliable[30] = {}
Queue.Reliable[30] = table.create(256)
Events.Reliable[31] = {}
Queue.Reliable[31] = table.create(256)
Queue.Reliable[33] = table.create(256)
Queue.Reliable[34] = table.create(256)
Queue.Reliable[35] = table.create(256)
Queue.Reliable[36] = table.create(256)
Events.Reliable[37] = {}
Queue.Reliable[37] = table.create(256)
Events.Reliable[38] = {}
Queue.Reliable[38] = table.create(256)
Events.Reliable[39] = {}
Queue.Reliable[39] = table.create(256)
Queue.Reliable[40] = table.create(256)
Queue.Reliable[41] = table.create(256)
Queue.Reliable[42] = table.create(256)
Queue.Reliable[43] = table.create(256)
Queue.Reliable[44] = table.create(256)
Queue.Reliable[45] = table.create(256)
Queue.Reliable[46] = table.create(256)
Queue.Reliable[47] = table.create(256)
Events.Reliable[48] = {}
Queue.Reliable[48] = table.create(256)
export type components_Transform = { cframe: CFrame, doNotReconcile: boolean? }
export type components_Velocity = { velocity: Vector3 }
export type components_Health = { health: number, maxHealth: number, regenRate: number, causedBy: number? }
export type components_Team = { name: string }
export type components_MerryGoRound = { targetAngularVelocity: number, currentAngularVelocity: number, angularAcceleration: number, maxAngularVelocity: number }
export type components_Cooldown = { expiry: number }
export type components_Lifetime = { expiry: number }
export type components_WalkSpeed = { speed: number, modifier: number }
export type components_Item = { Id: number }
export type components_Identifier = { uuid: string }
export type components_VoxelGrid = { data: buffer, sizeX: number, sizeY: number, sizeZ: number, cellSize: number }
export type components_Killed = { killerEntityId: number, markedKill: boolean?, expiry: number?, processRemoval: boolean? }
export type components_Knocked = { direction: Vector3, strength: number, applied: boolean, expiry: number? }
export type components_Gun = { LocalCooldownMillis: number?, ReloadTimeMillis: number?, Damage: number?, BulletLifeTime: number?, MaxCapacity: number?, ReloadTime: number?, CurrentCapacity: number?, BulletSpeed: number?, BulletSoundId: number?, KnockStrength: number?, Disabled: boolean?, Reloading: boolean?, VoxelDestructionRadius: number?, VoxelExplosionForce: number?, VoxelDebrisLifetime: number? }
export type components_Target = { CanTarget: boolean }
export type ReplecsPackage = { Changes: any, Variants: any }
export type ActionPayload = { actionId: string, action: string, velocity: Vector3?, origin: CFrame?, fromGun: number?, timestamp: number?, targetEntityId: number?, merryGoRoundId: number?, hitPosition: Vector3? }
export type NetworkResponse = { Success: boolean, Message: string?, Response: any? }
export type PlayerResources = { Coins: number, Gems: number, Level: number, Experience: number }
export type VotePayload = { field: string, choice: string }
export type PurchaseCratePayload = { crateType: string, quantity: number }
export type TradeItemPayload = { tradeUuid: string, itemUuid: string }
export type VoxelDeltaPayload = { entityId: number, indices: buffer, hitPosition: Vector3, explosionForce: number, debrisLifetime: number }
function SerdesFunctions.ReadEVENT_ReplecsUpdate(): ({ Changes: any, Variants: any })
	local Value = {} :: any
	RecieveInstanceCursor += 1
	Value.Changes = RecieveInstances[RecieveInstanceCursor]
	RecieveInstanceCursor += 1
	Value.Variants = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_ReplecsUnreliableUpdate(): ({ Changes: any, Variants: any })
	local Value = {} :: any
	RecieveInstanceCursor += 1
	Value.Changes = RecieveInstances[RecieveInstanceCursor]
	RecieveInstanceCursor += 1
	Value.Variants = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.WriteEVENT_ReplecsHandshakeRequest(Value: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 1)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Value)
end

function SerdesFunctions.ReadEVENT_ReplecsHandshakeResponse(): ({ Changes: any, Variants: any })
	local Value = {} :: any
	RecieveInstanceCursor += 1
	Value.Changes = RecieveInstances[RecieveInstanceCursor]
	RecieveInstanceCursor += 1
	Value.Variants = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.WriteEVENT_ProcessAction(Value: { actionId: string, action: string, velocity: Vector3?, origin: CFrame?, fromGun: number?, timestamp: number?, targetEntityId: number?, merryGoRoundId: number?, hitPosition: Vector3? }): ()
	-- Allocate BLOCK: 10 bytes
	local BLOCK_START = Allocate(10)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 3)
	local Length = #Value.actionId
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value.actionId, Length)
	local Length = #Value.action
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value.action, Length)
	buffer.writeu8(SendBuffer, BLOCK_START + 3, Value.velocity ~= nil and 1 or 0)
	if Value.velocity ~= nil then
		-- Allocate BLOCK: 12 bytes
		local BLOCK_START = Allocate(12)
		local Vector = Value.velocity
		buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
		buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
		buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 4, Value.origin ~= nil and 1 or 0)
	if Value.origin ~= nil then
		-- Allocate BLOCK: 24 bytes
		local BLOCK_START = Allocate(24)
		local Vector = Value.origin.Position
		buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
		buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
		buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
		local rX, rY, rZ = Value.origin:ToOrientation()
		buffer.writef32(SendBuffer, BLOCK_START + 12, rX)
		buffer.writef32(SendBuffer, BLOCK_START + 16, rY)
		buffer.writef32(SendBuffer, BLOCK_START + 20, rZ)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 5, Value.fromGun ~= nil and 1 or 0)
	if Value.fromGun ~= nil then
		-- Allocate BLOCK: 8 bytes
		local BLOCK_START = Allocate(8)
		buffer.writef64(SendBuffer, BLOCK_START + 0, Value.fromGun)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 6, Value.timestamp ~= nil and 1 or 0)
	if Value.timestamp ~= nil then
		-- Allocate BLOCK: 8 bytes
		local BLOCK_START = Allocate(8)
		buffer.writef64(SendBuffer, BLOCK_START + 0, Value.timestamp)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 7, Value.targetEntityId ~= nil and 1 or 0)
	if Value.targetEntityId ~= nil then
		-- Allocate BLOCK: 8 bytes
		local BLOCK_START = Allocate(8)
		buffer.writef64(SendBuffer, BLOCK_START + 0, Value.targetEntityId)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 8, Value.merryGoRoundId ~= nil and 1 or 0)
	if Value.merryGoRoundId ~= nil then
		-- Allocate BLOCK: 8 bytes
		local BLOCK_START = Allocate(8)
		buffer.writef64(SendBuffer, BLOCK_START + 0, Value.merryGoRoundId)
	end
	buffer.writeu8(SendBuffer, BLOCK_START + 9, Value.hitPosition ~= nil and 1 or 0)
	if Value.hitPosition ~= nil then
		-- Allocate BLOCK: 12 bytes
		local BLOCK_START = Allocate(12)
		local Vector = Value.hitPosition
		buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
		buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
		buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
	end
end

function SerdesFunctions.ReadEVENT_ResourcesSync(): ({ Coins: number, Gems: number, Level: number, Experience: number })
	-- Read BLOCK: 32 bytes
	local BLOCK_START = Read(32)
	local Value = {} :: any
	Value.Coins = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
	Value.Gems = buffer.readf64(RecieveBuffer, BLOCK_START + 8)
	Value.Level = buffer.readf64(RecieveBuffer, BLOCK_START + 16)
	Value.Experience = buffer.readf64(RecieveBuffer, BLOCK_START + 24)
	return Value
end

function SerdesFunctions.ReadEVENT_SettingsSync(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_StatisticsSync(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_PlayerlistSync(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_InventorySync(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_RewardsSync(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_TradingActiveTradeSync(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_VotingSync(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_RoundStatusSync(): (string)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Length = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	if #Value < 0 then error(`Expected length of "Value" to be larger than 0, got {#Value} instead.`) end
	if #Value > 128 then error(`Expected length of "Value" to be smaller than 128, got {#Value} instead.`) end
	return Value
end

function SerdesFunctions.ReadEVENT_InventoryItemAdded(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_InventoryItemRemoved(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_InventoryEquipItem(Value: string, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 15)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.ReadFUNCTION_RETURN_InventoryEquipItem(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_InventoryUnequipItem(Value: string, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 16)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.ReadFUNCTION_RETURN_InventoryUnequipItem(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_InventoryLockItem(Value: string, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 17)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.ReadFUNCTION_RETURN_InventoryLockItem(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_InventoryUnlockItem(Value: string, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 18)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.ReadFUNCTION_RETURN_InventoryUnlockItem(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_InventoryToggleFavorite(Value1: string, Value2: boolean, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 4 bytes
	local BLOCK_START = Allocate(4)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 19)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value1
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value1, Length)
	buffer.writeu8(SendBuffer, BLOCK_START + 3, Value2 and 1 or 0)
end

function SerdesFunctions.ReadFUNCTION_RETURN_InventoryToggleFavorite(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_InventoryOpenCrate(Value: string, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 20)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.ReadFUNCTION_RETURN_InventoryOpenCrate(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_RewardsClaimDailyReward(Value: nil, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 21)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
end

function SerdesFunctions.ReadFUNCTION_RETURN_RewardsClaimDailyReward(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.ReadEVENT_AchievementsChanged(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_AchievementsClaimAchievement(Value: string, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 23)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.ReadFUNCTION_RETURN_AchievementsClaimAchievement(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_AchievementsGetAchievements(Value: nil, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 24)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
end

function SerdesFunctions.ReadFUNCTION_RETURN_AchievementsGetAchievements(): (any)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_RoundStartMatch(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_RoundEndMatch(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_RoundApplyTeamIndicator(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_RoundSendDistraction(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.WriteEVENT_VoteSubmit(Value: { field: string, choice: string }): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 29)
	local Length = #Value.field
	buffer.writeu8(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value.field, Length)
	local Length = #Value.choice
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value.choice, Length)
end

function SerdesFunctions.ReadEVENT_NotificationAdd(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_NotificationRemove(): (string)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Length = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	if #Value < 0 then error(`Expected length of "Value" to be larger than 0, got {#Value} instead.`) end
	if #Value > 64 then error(`Expected length of "Value" to be smaller than 64, got {#Value} instead.`) end
	return Value
end

function SerdesFunctions.WriteEVENT_ShopSetGiftPlayer(Value: Player?): ()
	-- Allocate BLOCK: 1 bytes
	local BLOCK_START = Allocate(1)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 32)
	table.insert(SendInstances, Value or Null)
end

function SerdesFunctions.WriteFUNCTION_DATA_ShopSubmitCode(Value: string, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 33)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.ReadFUNCTION_RETURN_ShopSubmitCode(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_ShopPurchaseCrate(Value: { crateType: string, quantity: number }, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 5 bytes
	local BLOCK_START = Allocate(5)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 34)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value.crateType
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value.crateType, Length)
	buffer.writeu16(SendBuffer, BLOCK_START + 3, Value.quantity)
end

function SerdesFunctions.ReadFUNCTION_RETURN_ShopPurchaseCrate(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_ShopGetGiftedGamepasses(Value: Player, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 35)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	table.insert(SendInstances, Value or Null)
end

function SerdesFunctions.ReadFUNCTION_RETURN_ShopGetGiftedGamepasses(): (any)
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_SettingsChangeSetting(Value1: string, Value2: any, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 36)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value1
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value1, Length)
	table.insert(SendInstances, if Value2 == nil then Null else Value2)
end

function SerdesFunctions.ReadFUNCTION_RETURN_SettingsChangeSetting(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.ReadEVENT_TradingTradeReceived(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_TradingTradeCompleted(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.ReadEVENT_TradingTradeProcessed(): (any)
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_TradingSendTradeToPlayer(Value: Player, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 2 bytes
	local BLOCK_START = Allocate(2)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 40)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	table.insert(SendInstances, Value or Null)
end

function SerdesFunctions.ReadFUNCTION_RETURN_TradingSendTradeToPlayer(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_TradingAcceptTradeRequest(Value: string, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 41)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.ReadFUNCTION_RETURN_TradingAcceptTradeRequest(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_TradingDeclineTradeRequest(Value: string, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 42)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.ReadFUNCTION_RETURN_TradingDeclineTradeRequest(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_TradingAddItem(Value: { tradeUuid: string, itemUuid: string }, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 4 bytes
	local BLOCK_START = Allocate(4)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 43)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value.tradeUuid
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value.tradeUuid, Length)
	local Length = #Value.itemUuid
	buffer.writeu8(SendBuffer, BLOCK_START + 3, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value.itemUuid, Length)
end

function SerdesFunctions.ReadFUNCTION_RETURN_TradingAddItem(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_TradingRemoveItem(Value: { tradeUuid: string, itemUuid: string }, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 4 bytes
	local BLOCK_START = Allocate(4)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 44)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value.tradeUuid
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value.tradeUuid, Length)
	local Length = #Value.itemUuid
	buffer.writeu8(SendBuffer, BLOCK_START + 3, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value.itemUuid, Length)
end

function SerdesFunctions.ReadFUNCTION_RETURN_TradingRemoveItem(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_TradingAcceptTrade(Value: string, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 45)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.ReadFUNCTION_RETURN_TradingAcceptTrade(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_TradingDeclineTrade(Value: string, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 46)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.ReadFUNCTION_RETURN_TradingDeclineTrade(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.WriteFUNCTION_DATA_TradingConfirmTrade(Value: string, InvocationIdentifier: number): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 47)
	buffer.writeu8(SendBuffer, BLOCK_START + 1, InvocationIdentifier)
	local Length = #Value
	buffer.writeu8(SendBuffer, BLOCK_START + 2, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

function SerdesFunctions.ReadFUNCTION_RETURN_TradingConfirmTrade(): ({ Success: boolean, Message: string?, Response: any? })
	-- Read BLOCK: 4 bytes
	local BLOCK_START = Read(4)
	local Success = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Success ~= 1 then
		error("Server encountered an error.")
	end
	local Value = {} :: any
	Value.Success = (buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1)
	if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
		-- Read BLOCK: 2 bytes
		local BLOCK_START = Read(2)
		local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
		Value.Message = buffer.readstring(RecieveBuffer, Read(Length), Length)
		if #Value.Message < 0 then error(`Expected length of "Value.Message" to be larger than 0, got {#Value.Message} instead.`) end
		if #Value.Message > 256 then error(`Expected length of "Value.Message" to be smaller than 256, got {#Value.Message} instead.`) end
	end
	if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
		RecieveInstanceCursor += 1
		Value.Response = RecieveInstances[RecieveInstanceCursor]
	end
	return Value
end

function SerdesFunctions.ReadEVENT_VoxelDestroyCells(): ({ entityId: number, indices: buffer, hitPosition: Vector3, explosionForce: number, debrisLifetime: number })
	-- Read BLOCK: 30 bytes
	local BLOCK_START = Read(30)
	local Value = {} :: any
	Value.entityId = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 8)
	Value.indices = buffer.create(Length)
	buffer.copy(Value.indices, 0, RecieveBuffer, Read(Length), Length)
	local X = buffer.readf32(RecieveBuffer, BLOCK_START + 10)
	local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 14)
	local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 18)
	Value.hitPosition = Vector3.new(X, Y, Z)
	Value.explosionForce = buffer.readf32(RecieveBuffer, BLOCK_START + 22)
	Value.debrisLifetime = buffer.readf32(RecieveBuffer, BLOCK_START + 26)
	return Value
end

if not RunService:IsRunning() then
	local NOOP = function() end
	local Returns = table.freeze({
	components = {
		Transform = {
			Read = function(Buffer: buffer): (components_Transform)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 25 bytes
				local BLOCK_START = Read(25)
				-- Buffer read
				local Value = {} :: any
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				local Position = Vector3.new(X, Y, Z)
				local rX = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
				local rY = buffer.readf32(RecieveBuffer, BLOCK_START + 16)
				local rZ = buffer.readf32(RecieveBuffer, BLOCK_START + 20)
				Value.cframe = CFrame.new(Position) * CFrame.fromOrientation(rX, rY, rZ)
				if buffer.readu8(RecieveBuffer, BLOCK_START + 24) == 1 then
					-- Read BLOCK: 1 bytes
					local BLOCK_START = Read(1)
					Value.doNotReconcile = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
				end
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Transform): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 25 bytes
				local BLOCK_START = Allocate(25)
				-- Buffer write
				local Vector = Value.cframe.Position
				buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
				local rX, rY, rZ = Value.cframe:ToOrientation()
				buffer.writef32(SendBuffer, BLOCK_START + 12, rX)
				buffer.writef32(SendBuffer, BLOCK_START + 16, rY)
				buffer.writef32(SendBuffer, BLOCK_START + 20, rZ)
				buffer.writeu8(SendBuffer, BLOCK_START + 24, Value.doNotReconcile ~= nil and 1 or 0)
				if Value.doNotReconcile ~= nil then
					-- Allocate BLOCK: 1 bytes
					local BLOCK_START = Allocate(1)
					buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.doNotReconcile and 1 or 0)
				end
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Velocity = {
			Read = function(Buffer: buffer): (components_Velocity)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 12 bytes
				local BLOCK_START = Read(12)
				-- Buffer read
				local Value = {} :: any
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				Value.velocity = Vector3.new(X, Y, Z)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Velocity): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 12 bytes
				local BLOCK_START = Allocate(12)
				-- Buffer write
				local Vector = Value.velocity
				buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Health = {
			Read = function(Buffer: buffer): (components_Health)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 9 bytes
				local BLOCK_START = Read(9)
				-- Buffer read
				local Value = {} :: any
				Value.health = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
				Value.maxHealth = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
				Value.regenRate = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				if buffer.readu8(RecieveBuffer, BLOCK_START + 8) == 1 then
					-- Read BLOCK: 8 bytes
					local BLOCK_START = Read(8)
					Value.causedBy = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
				end
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Health): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 9 bytes
				local BLOCK_START = Allocate(9)
				-- Buffer write
				buffer.writeu16(SendBuffer, BLOCK_START + 0, Value.health)
				buffer.writeu16(SendBuffer, BLOCK_START + 2, Value.maxHealth)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Value.regenRate)
				buffer.writeu8(SendBuffer, BLOCK_START + 8, Value.causedBy ~= nil and 1 or 0)
				if Value.causedBy ~= nil then
					-- Allocate BLOCK: 8 bytes
					local BLOCK_START = Allocate(8)
					buffer.writef64(SendBuffer, BLOCK_START + 0, Value.causedBy)
				end
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Team = {
			Read = function(Buffer: buffer): (components_Team)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 1 bytes
				local BLOCK_START = Read(1)
				-- Buffer read
				local Value = {} :: any
				local Length = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				Value.name = buffer.readstring(RecieveBuffer, Read(Length), Length)
				if #Value.name < 0 then error(`Expected length of "Value.name" to be larger than 0, got {#Value.name} instead.`) end
				if #Value.name > 32 then error(`Expected length of "Value.name" to be smaller than 32, got {#Value.name} instead.`) end
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Team): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 1 bytes
				local BLOCK_START = Allocate(1)
				-- Buffer write
				local Length = #Value.name
				buffer.writeu8(SendBuffer, BLOCK_START + 0, Length)
				Allocate(Length)
				buffer.writestring(SendBuffer, SendOffset, Value.name, Length)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		MerryGoRound = {
			Read = function(Buffer: buffer): (components_MerryGoRound)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 16 bytes
				local BLOCK_START = Read(16)
				-- Buffer read
				local Value = {} :: any
				Value.targetAngularVelocity = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				Value.currentAngularVelocity = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				Value.angularAcceleration = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				Value.maxAngularVelocity = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_MerryGoRound): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 16 bytes
				local BLOCK_START = Allocate(16)
				-- Buffer write
				buffer.writef32(SendBuffer, BLOCK_START + 0, Value.targetAngularVelocity)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Value.currentAngularVelocity)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Value.angularAcceleration)
				buffer.writef32(SendBuffer, BLOCK_START + 12, Value.maxAngularVelocity)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Cooldown = {
			Read = function(Buffer: buffer): (components_Cooldown)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 8 bytes
				local BLOCK_START = Read(8)
				-- Buffer read
				local Value = {} :: any
				Value.expiry = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Cooldown): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 8 bytes
				local BLOCK_START = Allocate(8)
				-- Buffer write
				buffer.writef64(SendBuffer, BLOCK_START + 0, Value.expiry)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Lifetime = {
			Read = function(Buffer: buffer): (components_Lifetime)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 8 bytes
				local BLOCK_START = Read(8)
				-- Buffer read
				local Value = {} :: any
				Value.expiry = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Lifetime): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 8 bytes
				local BLOCK_START = Allocate(8)
				-- Buffer write
				buffer.writef64(SendBuffer, BLOCK_START + 0, Value.expiry)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		WalkSpeed = {
			Read = function(Buffer: buffer): (components_WalkSpeed)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 8 bytes
				local BLOCK_START = Read(8)
				-- Buffer read
				local Value = {} :: any
				Value.speed = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				Value.modifier = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_WalkSpeed): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 8 bytes
				local BLOCK_START = Allocate(8)
				-- Buffer write
				buffer.writef32(SendBuffer, BLOCK_START + 0, Value.speed)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Value.modifier)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Item = {
			Read = function(Buffer: buffer): (components_Item)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 4 bytes
				local BLOCK_START = Read(4)
				-- Buffer read
				local Value = {} :: any
				Value.Id = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Item): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 4 bytes
				local BLOCK_START = Allocate(4)
				-- Buffer write
				buffer.writeu32(SendBuffer, BLOCK_START + 0, Value.Id)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Identifier = {
			Read = function(Buffer: buffer): (components_Identifier)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 1 bytes
				local BLOCK_START = Read(1)
				-- Buffer read
				local Value = {} :: any
				local Length = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				Value.uuid = buffer.readstring(RecieveBuffer, Read(Length), Length)
				if #Value.uuid < 0 then error(`Expected length of "Value.uuid" to be larger than 0, got {#Value.uuid} instead.`) end
				if #Value.uuid > 64 then error(`Expected length of "Value.uuid" to be smaller than 64, got {#Value.uuid} instead.`) end
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Identifier): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 1 bytes
				local BLOCK_START = Allocate(1)
				-- Buffer write
				local Length = #Value.uuid
				buffer.writeu8(SendBuffer, BLOCK_START + 0, Length)
				Allocate(Length)
				buffer.writestring(SendBuffer, SendOffset, Value.uuid, Length)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		VoxelGrid = {
			Read = function(Buffer: buffer): (components_VoxelGrid)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 12 bytes
				local BLOCK_START = Read(12)
				-- Buffer read
				local Value = {} :: any
				local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
				Value.data = buffer.create(Length)
				buffer.copy(Value.data, 0, RecieveBuffer, Read(Length), Length)
				Value.sizeX = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
				Value.sizeY = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
				Value.sizeZ = buffer.readu16(RecieveBuffer, BLOCK_START + 6)
				Value.cellSize = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_VoxelGrid): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 12 bytes
				local BLOCK_START = Allocate(12)
				-- Buffer write
				local Length = buffer.len(Value.data)
				buffer.writeu16(SendBuffer, BLOCK_START + 0, Length)
				Allocate(Length)
				buffer.copy(SendBuffer, SendOffset, Value.data, 0, Length)
				buffer.writeu16(SendBuffer, BLOCK_START + 2, Value.sizeX)
				buffer.writeu16(SendBuffer, BLOCK_START + 4, Value.sizeY)
				buffer.writeu16(SendBuffer, BLOCK_START + 6, Value.sizeZ)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Value.cellSize)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Killed = {
			Read = function(Buffer: buffer): (components_Killed)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 11 bytes
				local BLOCK_START = Read(11)
				-- Buffer read
				local Value = {} :: any
				Value.killerEntityId = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
				if buffer.readu8(RecieveBuffer, BLOCK_START + 8) == 1 then
					-- Read BLOCK: 1 bytes
					local BLOCK_START = Read(1)
					Value.markedKill = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 9) == 1 then
					-- Read BLOCK: 8 bytes
					local BLOCK_START = Read(8)
					Value.expiry = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 10) == 1 then
					-- Read BLOCK: 1 bytes
					local BLOCK_START = Read(1)
					Value.processRemoval = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
				end
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Killed): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 11 bytes
				local BLOCK_START = Allocate(11)
				-- Buffer write
				buffer.writef64(SendBuffer, BLOCK_START + 0, Value.killerEntityId)
				buffer.writeu8(SendBuffer, BLOCK_START + 8, Value.markedKill ~= nil and 1 or 0)
				if Value.markedKill ~= nil then
					-- Allocate BLOCK: 1 bytes
					local BLOCK_START = Allocate(1)
					buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.markedKill and 1 or 0)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 9, Value.expiry ~= nil and 1 or 0)
				if Value.expiry ~= nil then
					-- Allocate BLOCK: 8 bytes
					local BLOCK_START = Allocate(8)
					buffer.writef64(SendBuffer, BLOCK_START + 0, Value.expiry)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 10, Value.processRemoval ~= nil and 1 or 0)
				if Value.processRemoval ~= nil then
					-- Allocate BLOCK: 1 bytes
					local BLOCK_START = Allocate(1)
					buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.processRemoval and 1 or 0)
				end
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Knocked = {
			Read = function(Buffer: buffer): (components_Knocked)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 18 bytes
				local BLOCK_START = Read(18)
				-- Buffer read
				local Value = {} :: any
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				Value.direction = Vector3.new(X, Y, Z)
				Value.strength = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
				Value.applied = (buffer.readu8(RecieveBuffer, BLOCK_START + 16) == 1)
				if buffer.readu8(RecieveBuffer, BLOCK_START + 17) == 1 then
					-- Read BLOCK: 8 bytes
					local BLOCK_START = Read(8)
					Value.expiry = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
				end
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Knocked): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 18 bytes
				local BLOCK_START = Allocate(18)
				-- Buffer write
				local Vector = Value.direction
				buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
				buffer.writef32(SendBuffer, BLOCK_START + 12, Value.strength)
				buffer.writeu8(SendBuffer, BLOCK_START + 16, Value.applied and 1 or 0)
				buffer.writeu8(SendBuffer, BLOCK_START + 17, Value.expiry ~= nil and 1 or 0)
				if Value.expiry ~= nil then
					-- Allocate BLOCK: 8 bytes
					local BLOCK_START = Allocate(8)
					buffer.writef64(SendBuffer, BLOCK_START + 0, Value.expiry)
				end
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Gun = {
			Read = function(Buffer: buffer): (components_Gun)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 15 bytes
				local BLOCK_START = Read(15)
				-- Buffer read
				local Value = {} :: any
				if buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.LocalCooldownMillis = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.ReloadTimeMillis = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.Damage = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.BulletLifeTime = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 4) == 1 then
					-- Read BLOCK: 2 bytes
					local BLOCK_START = Read(2)
					Value.MaxCapacity = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 5) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.ReloadTime = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 6) == 1 then
					-- Read BLOCK: 2 bytes
					local BLOCK_START = Read(2)
					Value.CurrentCapacity = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 7) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.BulletSpeed = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 8) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.BulletSoundId = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 9) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.KnockStrength = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 10) == 1 then
					-- Read BLOCK: 1 bytes
					local BLOCK_START = Read(1)
					Value.Disabled = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 11) == 1 then
					-- Read BLOCK: 1 bytes
					local BLOCK_START = Read(1)
					Value.Reloading = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 12) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.VoxelDestructionRadius = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 13) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.VoxelExplosionForce = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 14) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.VoxelDebrisLifetime = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Gun): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 15 bytes
				local BLOCK_START = Allocate(15)
				-- Buffer write
				buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.LocalCooldownMillis ~= nil and 1 or 0)
				if Value.LocalCooldownMillis ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.LocalCooldownMillis)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 1, Value.ReloadTimeMillis ~= nil and 1 or 0)
				if Value.ReloadTimeMillis ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.ReloadTimeMillis)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 2, Value.Damage ~= nil and 1 or 0)
				if Value.Damage ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.Damage)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 3, Value.BulletLifeTime ~= nil and 1 or 0)
				if Value.BulletLifeTime ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.BulletLifeTime)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 4, Value.MaxCapacity ~= nil and 1 or 0)
				if Value.MaxCapacity ~= nil then
					-- Allocate BLOCK: 2 bytes
					local BLOCK_START = Allocate(2)
					buffer.writeu16(SendBuffer, BLOCK_START + 0, Value.MaxCapacity)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 5, Value.ReloadTime ~= nil and 1 or 0)
				if Value.ReloadTime ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.ReloadTime)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 6, Value.CurrentCapacity ~= nil and 1 or 0)
				if Value.CurrentCapacity ~= nil then
					-- Allocate BLOCK: 2 bytes
					local BLOCK_START = Allocate(2)
					buffer.writeu16(SendBuffer, BLOCK_START + 0, Value.CurrentCapacity)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 7, Value.BulletSpeed ~= nil and 1 or 0)
				if Value.BulletSpeed ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.BulletSpeed)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 8, Value.BulletSoundId ~= nil and 1 or 0)
				if Value.BulletSoundId ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writeu32(SendBuffer, BLOCK_START + 0, Value.BulletSoundId)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 9, Value.KnockStrength ~= nil and 1 or 0)
				if Value.KnockStrength ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.KnockStrength)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 10, Value.Disabled ~= nil and 1 or 0)
				if Value.Disabled ~= nil then
					-- Allocate BLOCK: 1 bytes
					local BLOCK_START = Allocate(1)
					buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.Disabled and 1 or 0)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 11, Value.Reloading ~= nil and 1 or 0)
				if Value.Reloading ~= nil then
					-- Allocate BLOCK: 1 bytes
					local BLOCK_START = Allocate(1)
					buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.Reloading and 1 or 0)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 12, Value.VoxelDestructionRadius ~= nil and 1 or 0)
				if Value.VoxelDestructionRadius ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.VoxelDestructionRadius)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 13, Value.VoxelExplosionForce ~= nil and 1 or 0)
				if Value.VoxelExplosionForce ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.VoxelExplosionForce)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 14, Value.VoxelDebrisLifetime ~= nil and 1 or 0)
				if Value.VoxelDebrisLifetime ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.VoxelDebrisLifetime)
				end
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Target = {
			Read = function(Buffer: buffer): (components_Target)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 1 bytes
				local BLOCK_START = Read(1)
				-- Buffer read
				local Value = {} :: any
				Value.CanTarget = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Target): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 1 bytes
				local BLOCK_START = Allocate(1)
				-- Buffer write
				buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.CanTarget and 1 or 0)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
	},
	ReplecsUpdate = {
		On = NOOP
	},
	ReplecsUnreliableUpdate = {
		On = NOOP
	},
	ReplecsHandshakeRequest = {
		Fire = NOOP
	},
	ReplecsHandshakeResponse = {
		On = NOOP
	},
	ActionPayload = {
		Read = function(Buffer: buffer): (ActionPayload)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 9 bytes
			local BLOCK_START = Read(9)
			-- Buffer read
			local Value = {} :: any
			local Length = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
			Value.actionId = buffer.readstring(RecieveBuffer, Read(Length), Length)
			if #Value.actionId < 0 then error(`Expected length of "Value.actionId" to be larger than 0, got {#Value.actionId} instead.`) end
			if #Value.actionId > 64 then error(`Expected length of "Value.actionId" to be smaller than 64, got {#Value.actionId} instead.`) end
			Length = buffer.readu8(RecieveBuffer, BLOCK_START + 1)
			Value.action = buffer.readstring(RecieveBuffer, Read(Length), Length)
			if #Value.action < 0 then error(`Expected length of "Value.action" to be larger than 0, got {#Value.action} instead.`) end
			if #Value.action > 32 then error(`Expected length of "Value.action" to be smaller than 32, got {#Value.action} instead.`) end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
				-- Read BLOCK: 12 bytes
				local BLOCK_START = Read(12)
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				Value.velocity = Vector3.new(X, Y, Z)
			end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
				-- Read BLOCK: 24 bytes
				local BLOCK_START = Read(24)
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				local Position = Vector3.new(X, Y, Z)
				local rX = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
				local rY = buffer.readf32(RecieveBuffer, BLOCK_START + 16)
				local rZ = buffer.readf32(RecieveBuffer, BLOCK_START + 20)
				Value.origin = CFrame.new(Position) * CFrame.fromOrientation(rX, rY, rZ)
			end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 4) == 1 then
				-- Read BLOCK: 8 bytes
				local BLOCK_START = Read(8)
				Value.fromGun = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
			end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 5) == 1 then
				-- Read BLOCK: 8 bytes
				local BLOCK_START = Read(8)
				Value.timestamp = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
			end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 6) == 1 then
				-- Read BLOCK: 8 bytes
				local BLOCK_START = Read(8)
				Value.targetEntityId = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
			end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 7) == 1 then
				-- Read BLOCK: 8 bytes
				local BLOCK_START = Read(8)
				Value.merryGoRoundId = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
			end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 8) == 1 then
				-- Read BLOCK: 12 bytes
				local BLOCK_START = Read(12)
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				Value.hitPosition = Vector3.new(X, Y, Z)
			end
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: ActionPayload): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 9 bytes
			local BLOCK_START = Allocate(9)
			-- Buffer write
			local Length = #Value.actionId
			buffer.writeu8(SendBuffer, BLOCK_START + 0, Length)
			Allocate(Length)
			buffer.writestring(SendBuffer, SendOffset, Value.actionId, Length)
			local Length = #Value.action
			buffer.writeu8(SendBuffer, BLOCK_START + 1, Length)
			Allocate(Length)
			buffer.writestring(SendBuffer, SendOffset, Value.action, Length)
			buffer.writeu8(SendBuffer, BLOCK_START + 2, Value.velocity ~= nil and 1 or 0)
			if Value.velocity ~= nil then
				-- Allocate BLOCK: 12 bytes
				local BLOCK_START = Allocate(12)
				local Vector = Value.velocity
				buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
			end
			buffer.writeu8(SendBuffer, BLOCK_START + 3, Value.origin ~= nil and 1 or 0)
			if Value.origin ~= nil then
				-- Allocate BLOCK: 24 bytes
				local BLOCK_START = Allocate(24)
				local Vector = Value.origin.Position
				buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
				local rX, rY, rZ = Value.origin:ToOrientation()
				buffer.writef32(SendBuffer, BLOCK_START + 12, rX)
				buffer.writef32(SendBuffer, BLOCK_START + 16, rY)
				buffer.writef32(SendBuffer, BLOCK_START + 20, rZ)
			end
			buffer.writeu8(SendBuffer, BLOCK_START + 4, Value.fromGun ~= nil and 1 or 0)
			if Value.fromGun ~= nil then
				-- Allocate BLOCK: 8 bytes
				local BLOCK_START = Allocate(8)
				buffer.writef64(SendBuffer, BLOCK_START + 0, Value.fromGun)
			end
			buffer.writeu8(SendBuffer, BLOCK_START + 5, Value.timestamp ~= nil and 1 or 0)
			if Value.timestamp ~= nil then
				-- Allocate BLOCK: 8 bytes
				local BLOCK_START = Allocate(8)
				buffer.writef64(SendBuffer, BLOCK_START + 0, Value.timestamp)
			end
			buffer.writeu8(SendBuffer, BLOCK_START + 6, Value.targetEntityId ~= nil and 1 or 0)
			if Value.targetEntityId ~= nil then
				-- Allocate BLOCK: 8 bytes
				local BLOCK_START = Allocate(8)
				buffer.writef64(SendBuffer, BLOCK_START + 0, Value.targetEntityId)
			end
			buffer.writeu8(SendBuffer, BLOCK_START + 7, Value.merryGoRoundId ~= nil and 1 or 0)
			if Value.merryGoRoundId ~= nil then
				-- Allocate BLOCK: 8 bytes
				local BLOCK_START = Allocate(8)
				buffer.writef64(SendBuffer, BLOCK_START + 0, Value.merryGoRoundId)
			end
			buffer.writeu8(SendBuffer, BLOCK_START + 8, Value.hitPosition ~= nil and 1 or 0)
			if Value.hitPosition ~= nil then
				-- Allocate BLOCK: 12 bytes
				local BLOCK_START = Allocate(12)
				local Vector = Value.hitPosition
				buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
			end
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	ProcessAction = {
		Fire = NOOP
	},
	ResourcesSync = {
		On = NOOP
	},
	SettingsSync = {
		On = NOOP
	},
	StatisticsSync = {
		On = NOOP
	},
	PlayerlistSync = {
		On = NOOP
	},
	InventorySync = {
		On = NOOP
	},
	RewardsSync = {
		On = NOOP
	},
	TradingActiveTradeSync = {
		On = NOOP
	},
	VotingSync = {
		On = NOOP
	},
	RoundStatusSync = {
		On = NOOP
	},
	InventoryItemAdded = {
		On = NOOP
	},
	InventoryItemRemoved = {
		On = NOOP
	},
	InventoryEquipItem = {
		Invoke = NOOP
	},
	InventoryUnequipItem = {
		Invoke = NOOP
	},
	InventoryLockItem = {
		Invoke = NOOP
	},
	InventoryUnlockItem = {
		Invoke = NOOP
	},
	InventoryToggleFavorite = {
		Invoke = NOOP
	},
	InventoryOpenCrate = {
		Invoke = NOOP
	},
	RewardsClaimDailyReward = {
		Invoke = NOOP
	},
	AchievementsChanged = {
		On = NOOP
	},
	AchievementsClaimAchievement = {
		Invoke = NOOP
	},
	AchievementsGetAchievements = {
		Invoke = NOOP
	},
	RoundStartMatch = {
		On = NOOP
	},
	RoundEndMatch = {
		On = NOOP
	},
	RoundApplyTeamIndicator = {
		On = NOOP
	},
	RoundSendDistraction = {
		On = NOOP
	},
	VoteSubmit = {
		Fire = NOOP
	},
	NotificationAdd = {
		On = NOOP
	},
	NotificationRemove = {
		On = NOOP
	},
	ShopSetGiftPlayer = {
		Fire = NOOP
	},
	ShopSubmitCode = {
		Invoke = NOOP
	},
	ShopPurchaseCrate = {
		Invoke = NOOP
	},
	ShopGetGiftedGamepasses = {
		Invoke = NOOP
	},
	SettingsChangeSetting = {
		Invoke = NOOP
	},
	TradingTradeReceived = {
		On = NOOP
	},
	TradingTradeCompleted = {
		On = NOOP
	},
	TradingTradeProcessed = {
		On = NOOP
	},
	TradingSendTradeToPlayer = {
		Invoke = NOOP
	},
	TradingAcceptTradeRequest = {
		Invoke = NOOP
	},
	TradingDeclineTradeRequest = {
		Invoke = NOOP
	},
	TradingAddItem = {
		Invoke = NOOP
	},
	TradingRemoveItem = {
		Invoke = NOOP
	},
	TradingAcceptTrade = {
		Invoke = NOOP
	},
	TradingDeclineTrade = {
		Invoke = NOOP
	},
	TradingConfirmTrade = {
		Invoke = NOOP
	},
	VoxelDestroyCells = {
		On = NOOP
	},
	})
	return Returns :: BLINK_EVENTS_SYMBOL
end

if not RunService:IsClient() then
    error("Client network module can only be required from the client.")
end

local Reliable: RemoteEvent = ReplicatedStorage:WaitForChild(BASE_EVENT_NAME .. "_RELIABLE_REMOTE") :: RemoteEvent
local Unreliable: UnreliableRemoteEvent = ReplicatedStorage:WaitForChild(BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE") :: UnreliableRemoteEvent

local function StepReplication()
    if SendCursor <= 0 then
        return
    end

    local Buffer = buffer.create(SendCursor)
    buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
    Reliable:FireServer(Buffer, SendInstances)

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    table.clear(SendInstances)
end
RunService.Heartbeat:Connect(StepReplication)
Reliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 0 then
			local Value: { Changes: any, Variants: any } = SerdesFunctions.ReadEVENT_ReplecsUpdate()
			if #Events.Reliable[0] > 0 then
				for _, Listener in Events.Reliable[0] do
					SerdesEvent = "ReplecsUpdate"
					Listener(Value)
				end
			else
				if #Queue.Reliable[0] > 256 then
					warn("[Blink]: Event queue of \"ReplecsUpdate\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[0], {Value} :: {any})
			end
		elseif Index == 2 then
			local Value: { Changes: any, Variants: any } = SerdesFunctions.ReadEVENT_ReplecsHandshakeResponse()
			if Events.Reliable[2] ~= nil then
				SerdesEvent = "ReplecsHandshakeResponse"
				Events.Reliable[2](Value)
			else
				if #Queue.Reliable[2] > 256 then
					warn("[Blink]: Event queue of \"ReplecsHandshakeResponse\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[2], {Value} :: {any})
			end
		elseif Index == 4 then
			local Value: { Coins: number, Gems: number, Level: number, Experience: number } = SerdesFunctions.ReadEVENT_ResourcesSync()
			if Events.Reliable[4] ~= nil then
				task.spawn(Events.Reliable[4], Value)
			else
				if #Queue.Reliable[4] > 256 then
					warn("[Blink]: Event queue of \"ResourcesSync\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[4], {Value} :: {any})
			end
		elseif Index == 5 then
			local Value: any = SerdesFunctions.ReadEVENT_SettingsSync()
			if Events.Reliable[5] ~= nil then
				task.spawn(Events.Reliable[5], Value)
			else
				if #Queue.Reliable[5] > 256 then
					warn("[Blink]: Event queue of \"SettingsSync\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[5], {Value} :: {any})
			end
		elseif Index == 6 then
			local Value: any = SerdesFunctions.ReadEVENT_StatisticsSync()
			if Events.Reliable[6] ~= nil then
				task.spawn(Events.Reliable[6], Value)
			else
				if #Queue.Reliable[6] > 256 then
					warn("[Blink]: Event queue of \"StatisticsSync\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[6], {Value} :: {any})
			end
		elseif Index == 7 then
			local Value: any = SerdesFunctions.ReadEVENT_PlayerlistSync()
			if #Events.Reliable[7] > 0 then
				for _, Listener in Events.Reliable[7] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[7] > 256 then
					warn("[Blink]: Event queue of \"PlayerlistSync\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[7], {Value} :: {any})
			end
		elseif Index == 8 then
			local Value: any = SerdesFunctions.ReadEVENT_InventorySync()
			if Events.Reliable[8] ~= nil then
				task.spawn(Events.Reliable[8], Value)
			else
				if #Queue.Reliable[8] > 256 then
					warn("[Blink]: Event queue of \"InventorySync\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[8], {Value} :: {any})
			end
		elseif Index == 9 then
			local Value: any = SerdesFunctions.ReadEVENT_RewardsSync()
			if Events.Reliable[9] ~= nil then
				task.spawn(Events.Reliable[9], Value)
			else
				if #Queue.Reliable[9] > 256 then
					warn("[Blink]: Event queue of \"RewardsSync\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[9], {Value} :: {any})
			end
		elseif Index == 10 then
			local Value: any = SerdesFunctions.ReadEVENT_TradingActiveTradeSync()
			if Events.Reliable[10] ~= nil then
				task.spawn(Events.Reliable[10], Value)
			else
				if #Queue.Reliable[10] > 256 then
					warn("[Blink]: Event queue of \"TradingActiveTradeSync\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[10], {Value} :: {any})
			end
		elseif Index == 11 then
			local Value: any = SerdesFunctions.ReadEVENT_VotingSync()
			if #Events.Reliable[11] > 0 then
				for _, Listener in Events.Reliable[11] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[11] > 256 then
					warn("[Blink]: Event queue of \"VotingSync\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[11], {Value} :: {any})
			end
		elseif Index == 12 then
			local Value: string = SerdesFunctions.ReadEVENT_RoundStatusSync()
			if #Events.Reliable[12] > 0 then
				for _, Listener in Events.Reliable[12] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[12] > 256 then
					warn("[Blink]: Event queue of \"RoundStatusSync\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[12], {Value} :: {any})
			end
		elseif Index == 13 then
			local Value: any = SerdesFunctions.ReadEVENT_InventoryItemAdded()
			if #Events.Reliable[13] > 0 then
				for _, Listener in Events.Reliable[13] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[13] > 256 then
					warn("[Blink]: Event queue of \"InventoryItemAdded\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[13], {Value} :: {any})
			end
		elseif Index == 14 then
			local Value: any = SerdesFunctions.ReadEVENT_InventoryItemRemoved()
			if #Events.Reliable[14] > 0 then
				for _, Listener in Events.Reliable[14] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[14] > 256 then
					warn("[Blink]: Event queue of \"InventoryItemRemoved\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[14], {Value} :: {any})
			end
		elseif Index == 15 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_InventoryEquipItem()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 16 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_InventoryUnequipItem()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 17 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_InventoryLockItem()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 18 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_InventoryUnlockItem()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 19 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_InventoryToggleFavorite()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 20 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_InventoryOpenCrate()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 21 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_RewardsClaimDailyReward()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 22 then
			local Value: any = SerdesFunctions.ReadEVENT_AchievementsChanged()
			if #Events.Reliable[22] > 0 then
				for _, Listener in Events.Reliable[22] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[22] > 256 then
					warn("[Blink]: Event queue of \"AchievementsChanged\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[22], {Value} :: {any})
			end
		elseif Index == 23 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_AchievementsClaimAchievement()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 24 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_AchievementsGetAchievements()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 25 then
			local Value: any = SerdesFunctions.ReadEVENT_RoundStartMatch()
			if #Events.Reliable[25] > 0 then
				for _, Listener in Events.Reliable[25] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[25] > 256 then
					warn("[Blink]: Event queue of \"RoundStartMatch\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[25], {Value} :: {any})
			end
		elseif Index == 26 then
			local Value: any = SerdesFunctions.ReadEVENT_RoundEndMatch()
			if #Events.Reliable[26] > 0 then
				for _, Listener in Events.Reliable[26] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[26] > 256 then
					warn("[Blink]: Event queue of \"RoundEndMatch\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[26], {Value} :: {any})
			end
		elseif Index == 27 then
			local Value: any = SerdesFunctions.ReadEVENT_RoundApplyTeamIndicator()
			if Events.Reliable[27] ~= nil then
				task.spawn(Events.Reliable[27], Value)
			else
				if #Queue.Reliable[27] > 256 then
					warn("[Blink]: Event queue of \"RoundApplyTeamIndicator\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[27], {Value} :: {any})
			end
		elseif Index == 28 then
			local Value: any = SerdesFunctions.ReadEVENT_RoundSendDistraction()
			if #Events.Reliable[28] > 0 then
				for _, Listener in Events.Reliable[28] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[28] > 256 then
					warn("[Blink]: Event queue of \"RoundSendDistraction\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[28], {Value} :: {any})
			end
		elseif Index == 30 then
			local Value: any = SerdesFunctions.ReadEVENT_NotificationAdd()
			if #Events.Reliable[30] > 0 then
				for _, Listener in Events.Reliable[30] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[30] > 256 then
					warn("[Blink]: Event queue of \"NotificationAdd\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[30], {Value} :: {any})
			end
		elseif Index == 31 then
			local Value: string = SerdesFunctions.ReadEVENT_NotificationRemove()
			if #Events.Reliable[31] > 0 then
				for _, Listener in Events.Reliable[31] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[31] > 256 then
					warn("[Blink]: Event queue of \"NotificationRemove\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[31], {Value} :: {any})
			end
		elseif Index == 33 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_ShopSubmitCode()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 34 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_ShopPurchaseCrate()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 35 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_ShopGetGiftedGamepasses()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 36 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_SettingsChangeSetting()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 37 then
			local Value: any = SerdesFunctions.ReadEVENT_TradingTradeReceived()
			if #Events.Reliable[37] > 0 then
				for _, Listener in Events.Reliable[37] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[37] > 256 then
					warn("[Blink]: Event queue of \"TradingTradeReceived\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[37], {Value} :: {any})
			end
		elseif Index == 38 then
			local Value: any = SerdesFunctions.ReadEVENT_TradingTradeCompleted()
			if #Events.Reliable[38] > 0 then
				for _, Listener in Events.Reliable[38] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[38] > 256 then
					warn("[Blink]: Event queue of \"TradingTradeCompleted\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[38], {Value} :: {any})
			end
		elseif Index == 39 then
			local Value: any = SerdesFunctions.ReadEVENT_TradingTradeProcessed()
			if #Events.Reliable[39] > 0 then
				for _, Listener in Events.Reliable[39] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[39] > 256 then
					warn("[Blink]: Event queue of \"TradingTradeProcessed\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[39], {Value} :: {any})
			end
		elseif Index == 40 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_TradingSendTradeToPlayer()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 41 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_TradingAcceptTradeRequest()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 42 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_TradingDeclineTradeRequest()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 43 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_TradingAddItem()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 44 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_TradingRemoveItem()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 45 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_TradingAcceptTrade()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 46 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_TradingDeclineTrade()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 47 then
			-- Read BLOCK: 1 bytes
			local BLOCK_START = Read(1)
			local InvocationIdentifier = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				local Success, ReturnValue = pcall(function ()
					return SerdesFunctions.ReadFUNCTION_RETURN_TradingConfirmTrade()
				end)
			if Calls[InvocationIdentifier] ~= nil then
				task.spawn(Calls[InvocationIdentifier], Success, ReturnValue)
				Calls[InvocationIdentifier] = nil
			end
		elseif Index == 48 then
			local Value: { entityId: number, indices: buffer, hitPosition: Vector3, explosionForce: number, debrisLifetime: number } = SerdesFunctions.ReadEVENT_VoxelDestroyCells()
			if #Events.Reliable[48] > 0 then
				for _, Listener in Events.Reliable[48] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[48] > 256 then
					warn("[Blink]: Event queue of \"VoxelDestroyCells\" exceeded 256, did you forget to implement a listener?")
				end
				table.insert(Queue.Reliable[48], {Value} :: {any})
			end
		end
	end
end)
Unreliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: {Instance})
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 0 then
			local Value: { Changes: any, Variants: any } = SerdesFunctions.ReadEVENT_ReplecsUnreliableUpdate()
			for _, Listener in Events.Unreliable[0] do
				SerdesEvent = "ReplecsUnreliableUpdate"
				Listener(Value)
			end
		end
	end
end)


local Returns = table.freeze({
	StepReplication = StepReplication,

	components = {
		Transform = {
			Read = function(Buffer: buffer): (components_Transform)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 25 bytes
				local BLOCK_START = Read(25)
				-- Buffer read
				local Value = {} :: any
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				local Position = Vector3.new(X, Y, Z)
				local rX = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
				local rY = buffer.readf32(RecieveBuffer, BLOCK_START + 16)
				local rZ = buffer.readf32(RecieveBuffer, BLOCK_START + 20)
				Value.cframe = CFrame.new(Position) * CFrame.fromOrientation(rX, rY, rZ)
				if buffer.readu8(RecieveBuffer, BLOCK_START + 24) == 1 then
					-- Read BLOCK: 1 bytes
					local BLOCK_START = Read(1)
					Value.doNotReconcile = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
				end
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Transform): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 25 bytes
				local BLOCK_START = Allocate(25)
				-- Buffer write
				local Vector = Value.cframe.Position
				buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
				local rX, rY, rZ = Value.cframe:ToOrientation()
				buffer.writef32(SendBuffer, BLOCK_START + 12, rX)
				buffer.writef32(SendBuffer, BLOCK_START + 16, rY)
				buffer.writef32(SendBuffer, BLOCK_START + 20, rZ)
				buffer.writeu8(SendBuffer, BLOCK_START + 24, Value.doNotReconcile ~= nil and 1 or 0)
				if Value.doNotReconcile ~= nil then
					-- Allocate BLOCK: 1 bytes
					local BLOCK_START = Allocate(1)
					buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.doNotReconcile and 1 or 0)
				end
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Velocity = {
			Read = function(Buffer: buffer): (components_Velocity)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 12 bytes
				local BLOCK_START = Read(12)
				-- Buffer read
				local Value = {} :: any
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				Value.velocity = Vector3.new(X, Y, Z)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Velocity): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 12 bytes
				local BLOCK_START = Allocate(12)
				-- Buffer write
				local Vector = Value.velocity
				buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Health = {
			Read = function(Buffer: buffer): (components_Health)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 9 bytes
				local BLOCK_START = Read(9)
				-- Buffer read
				local Value = {} :: any
				Value.health = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
				Value.maxHealth = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
				Value.regenRate = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				if buffer.readu8(RecieveBuffer, BLOCK_START + 8) == 1 then
					-- Read BLOCK: 8 bytes
					local BLOCK_START = Read(8)
					Value.causedBy = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
				end
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Health): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 9 bytes
				local BLOCK_START = Allocate(9)
				-- Buffer write
				buffer.writeu16(SendBuffer, BLOCK_START + 0, Value.health)
				buffer.writeu16(SendBuffer, BLOCK_START + 2, Value.maxHealth)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Value.regenRate)
				buffer.writeu8(SendBuffer, BLOCK_START + 8, Value.causedBy ~= nil and 1 or 0)
				if Value.causedBy ~= nil then
					-- Allocate BLOCK: 8 bytes
					local BLOCK_START = Allocate(8)
					buffer.writef64(SendBuffer, BLOCK_START + 0, Value.causedBy)
				end
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Team = {
			Read = function(Buffer: buffer): (components_Team)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 1 bytes
				local BLOCK_START = Read(1)
				-- Buffer read
				local Value = {} :: any
				local Length = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				Value.name = buffer.readstring(RecieveBuffer, Read(Length), Length)
				if #Value.name < 0 then error(`Expected length of "Value.name" to be larger than 0, got {#Value.name} instead.`) end
				if #Value.name > 32 then error(`Expected length of "Value.name" to be smaller than 32, got {#Value.name} instead.`) end
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Team): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 1 bytes
				local BLOCK_START = Allocate(1)
				-- Buffer write
				local Length = #Value.name
				buffer.writeu8(SendBuffer, BLOCK_START + 0, Length)
				Allocate(Length)
				buffer.writestring(SendBuffer, SendOffset, Value.name, Length)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		MerryGoRound = {
			Read = function(Buffer: buffer): (components_MerryGoRound)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 16 bytes
				local BLOCK_START = Read(16)
				-- Buffer read
				local Value = {} :: any
				Value.targetAngularVelocity = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				Value.currentAngularVelocity = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				Value.angularAcceleration = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				Value.maxAngularVelocity = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_MerryGoRound): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 16 bytes
				local BLOCK_START = Allocate(16)
				-- Buffer write
				buffer.writef32(SendBuffer, BLOCK_START + 0, Value.targetAngularVelocity)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Value.currentAngularVelocity)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Value.angularAcceleration)
				buffer.writef32(SendBuffer, BLOCK_START + 12, Value.maxAngularVelocity)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Cooldown = {
			Read = function(Buffer: buffer): (components_Cooldown)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 8 bytes
				local BLOCK_START = Read(8)
				-- Buffer read
				local Value = {} :: any
				Value.expiry = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Cooldown): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 8 bytes
				local BLOCK_START = Allocate(8)
				-- Buffer write
				buffer.writef64(SendBuffer, BLOCK_START + 0, Value.expiry)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Lifetime = {
			Read = function(Buffer: buffer): (components_Lifetime)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 8 bytes
				local BLOCK_START = Read(8)
				-- Buffer read
				local Value = {} :: any
				Value.expiry = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Lifetime): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 8 bytes
				local BLOCK_START = Allocate(8)
				-- Buffer write
				buffer.writef64(SendBuffer, BLOCK_START + 0, Value.expiry)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		WalkSpeed = {
			Read = function(Buffer: buffer): (components_WalkSpeed)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 8 bytes
				local BLOCK_START = Read(8)
				-- Buffer read
				local Value = {} :: any
				Value.speed = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				Value.modifier = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_WalkSpeed): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 8 bytes
				local BLOCK_START = Allocate(8)
				-- Buffer write
				buffer.writef32(SendBuffer, BLOCK_START + 0, Value.speed)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Value.modifier)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Item = {
			Read = function(Buffer: buffer): (components_Item)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 4 bytes
				local BLOCK_START = Read(4)
				-- Buffer read
				local Value = {} :: any
				Value.Id = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Item): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 4 bytes
				local BLOCK_START = Allocate(4)
				-- Buffer write
				buffer.writeu32(SendBuffer, BLOCK_START + 0, Value.Id)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Identifier = {
			Read = function(Buffer: buffer): (components_Identifier)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 1 bytes
				local BLOCK_START = Read(1)
				-- Buffer read
				local Value = {} :: any
				local Length = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				Value.uuid = buffer.readstring(RecieveBuffer, Read(Length), Length)
				if #Value.uuid < 0 then error(`Expected length of "Value.uuid" to be larger than 0, got {#Value.uuid} instead.`) end
				if #Value.uuid > 64 then error(`Expected length of "Value.uuid" to be smaller than 64, got {#Value.uuid} instead.`) end
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Identifier): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 1 bytes
				local BLOCK_START = Allocate(1)
				-- Buffer write
				local Length = #Value.uuid
				buffer.writeu8(SendBuffer, BLOCK_START + 0, Length)
				Allocate(Length)
				buffer.writestring(SendBuffer, SendOffset, Value.uuid, Length)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		VoxelGrid = {
			Read = function(Buffer: buffer): (components_VoxelGrid)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 12 bytes
				local BLOCK_START = Read(12)
				-- Buffer read
				local Value = {} :: any
				local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
				Value.data = buffer.create(Length)
				buffer.copy(Value.data, 0, RecieveBuffer, Read(Length), Length)
				Value.sizeX = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
				Value.sizeY = buffer.readu16(RecieveBuffer, BLOCK_START + 4)
				Value.sizeZ = buffer.readu16(RecieveBuffer, BLOCK_START + 6)
				Value.cellSize = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_VoxelGrid): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 12 bytes
				local BLOCK_START = Allocate(12)
				-- Buffer write
				local Length = buffer.len(Value.data)
				buffer.writeu16(SendBuffer, BLOCK_START + 0, Length)
				Allocate(Length)
				buffer.copy(SendBuffer, SendOffset, Value.data, 0, Length)
				buffer.writeu16(SendBuffer, BLOCK_START + 2, Value.sizeX)
				buffer.writeu16(SendBuffer, BLOCK_START + 4, Value.sizeY)
				buffer.writeu16(SendBuffer, BLOCK_START + 6, Value.sizeZ)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Value.cellSize)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Killed = {
			Read = function(Buffer: buffer): (components_Killed)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 11 bytes
				local BLOCK_START = Read(11)
				-- Buffer read
				local Value = {} :: any
				Value.killerEntityId = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
				if buffer.readu8(RecieveBuffer, BLOCK_START + 8) == 1 then
					-- Read BLOCK: 1 bytes
					local BLOCK_START = Read(1)
					Value.markedKill = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 9) == 1 then
					-- Read BLOCK: 8 bytes
					local BLOCK_START = Read(8)
					Value.expiry = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 10) == 1 then
					-- Read BLOCK: 1 bytes
					local BLOCK_START = Read(1)
					Value.processRemoval = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
				end
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Killed): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 11 bytes
				local BLOCK_START = Allocate(11)
				-- Buffer write
				buffer.writef64(SendBuffer, BLOCK_START + 0, Value.killerEntityId)
				buffer.writeu8(SendBuffer, BLOCK_START + 8, Value.markedKill ~= nil and 1 or 0)
				if Value.markedKill ~= nil then
					-- Allocate BLOCK: 1 bytes
					local BLOCK_START = Allocate(1)
					buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.markedKill and 1 or 0)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 9, Value.expiry ~= nil and 1 or 0)
				if Value.expiry ~= nil then
					-- Allocate BLOCK: 8 bytes
					local BLOCK_START = Allocate(8)
					buffer.writef64(SendBuffer, BLOCK_START + 0, Value.expiry)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 10, Value.processRemoval ~= nil and 1 or 0)
				if Value.processRemoval ~= nil then
					-- Allocate BLOCK: 1 bytes
					local BLOCK_START = Allocate(1)
					buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.processRemoval and 1 or 0)
				end
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Knocked = {
			Read = function(Buffer: buffer): (components_Knocked)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 18 bytes
				local BLOCK_START = Read(18)
				-- Buffer read
				local Value = {} :: any
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				Value.direction = Vector3.new(X, Y, Z)
				Value.strength = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
				Value.applied = (buffer.readu8(RecieveBuffer, BLOCK_START + 16) == 1)
				if buffer.readu8(RecieveBuffer, BLOCK_START + 17) == 1 then
					-- Read BLOCK: 8 bytes
					local BLOCK_START = Read(8)
					Value.expiry = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
				end
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Knocked): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 18 bytes
				local BLOCK_START = Allocate(18)
				-- Buffer write
				local Vector = Value.direction
				buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
				buffer.writef32(SendBuffer, BLOCK_START + 12, Value.strength)
				buffer.writeu8(SendBuffer, BLOCK_START + 16, Value.applied and 1 or 0)
				buffer.writeu8(SendBuffer, BLOCK_START + 17, Value.expiry ~= nil and 1 or 0)
				if Value.expiry ~= nil then
					-- Allocate BLOCK: 8 bytes
					local BLOCK_START = Allocate(8)
					buffer.writef64(SendBuffer, BLOCK_START + 0, Value.expiry)
				end
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Gun = {
			Read = function(Buffer: buffer): (components_Gun)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 15 bytes
				local BLOCK_START = Read(15)
				-- Buffer read
				local Value = {} :: any
				if buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.LocalCooldownMillis = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 1) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.ReloadTimeMillis = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.Damage = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.BulletLifeTime = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 4) == 1 then
					-- Read BLOCK: 2 bytes
					local BLOCK_START = Read(2)
					Value.MaxCapacity = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 5) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.ReloadTime = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 6) == 1 then
					-- Read BLOCK: 2 bytes
					local BLOCK_START = Read(2)
					Value.CurrentCapacity = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 7) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.BulletSpeed = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 8) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.BulletSoundId = buffer.readu32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 9) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.KnockStrength = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 10) == 1 then
					-- Read BLOCK: 1 bytes
					local BLOCK_START = Read(1)
					Value.Disabled = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 11) == 1 then
					-- Read BLOCK: 1 bytes
					local BLOCK_START = Read(1)
					Value.Reloading = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 12) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.VoxelDestructionRadius = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 13) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.VoxelExplosionForce = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				if buffer.readu8(RecieveBuffer, BLOCK_START + 14) == 1 then
					-- Read BLOCK: 4 bytes
					local BLOCK_START = Read(4)
					Value.VoxelDebrisLifetime = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				end
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Gun): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 15 bytes
				local BLOCK_START = Allocate(15)
				-- Buffer write
				buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.LocalCooldownMillis ~= nil and 1 or 0)
				if Value.LocalCooldownMillis ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.LocalCooldownMillis)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 1, Value.ReloadTimeMillis ~= nil and 1 or 0)
				if Value.ReloadTimeMillis ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.ReloadTimeMillis)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 2, Value.Damage ~= nil and 1 or 0)
				if Value.Damage ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.Damage)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 3, Value.BulletLifeTime ~= nil and 1 or 0)
				if Value.BulletLifeTime ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.BulletLifeTime)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 4, Value.MaxCapacity ~= nil and 1 or 0)
				if Value.MaxCapacity ~= nil then
					-- Allocate BLOCK: 2 bytes
					local BLOCK_START = Allocate(2)
					buffer.writeu16(SendBuffer, BLOCK_START + 0, Value.MaxCapacity)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 5, Value.ReloadTime ~= nil and 1 or 0)
				if Value.ReloadTime ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.ReloadTime)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 6, Value.CurrentCapacity ~= nil and 1 or 0)
				if Value.CurrentCapacity ~= nil then
					-- Allocate BLOCK: 2 bytes
					local BLOCK_START = Allocate(2)
					buffer.writeu16(SendBuffer, BLOCK_START + 0, Value.CurrentCapacity)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 7, Value.BulletSpeed ~= nil and 1 or 0)
				if Value.BulletSpeed ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.BulletSpeed)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 8, Value.BulletSoundId ~= nil and 1 or 0)
				if Value.BulletSoundId ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writeu32(SendBuffer, BLOCK_START + 0, Value.BulletSoundId)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 9, Value.KnockStrength ~= nil and 1 or 0)
				if Value.KnockStrength ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.KnockStrength)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 10, Value.Disabled ~= nil and 1 or 0)
				if Value.Disabled ~= nil then
					-- Allocate BLOCK: 1 bytes
					local BLOCK_START = Allocate(1)
					buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.Disabled and 1 or 0)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 11, Value.Reloading ~= nil and 1 or 0)
				if Value.Reloading ~= nil then
					-- Allocate BLOCK: 1 bytes
					local BLOCK_START = Allocate(1)
					buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.Reloading and 1 or 0)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 12, Value.VoxelDestructionRadius ~= nil and 1 or 0)
				if Value.VoxelDestructionRadius ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.VoxelDestructionRadius)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 13, Value.VoxelExplosionForce ~= nil and 1 or 0)
				if Value.VoxelExplosionForce ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.VoxelExplosionForce)
				end
				buffer.writeu8(SendBuffer, BLOCK_START + 14, Value.VoxelDebrisLifetime ~= nil and 1 or 0)
				if Value.VoxelDebrisLifetime ~= nil then
					-- Allocate BLOCK: 4 bytes
					local BLOCK_START = Allocate(4)
					buffer.writef32(SendBuffer, BLOCK_START + 0, Value.VoxelDebrisLifetime)
				end
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Target = {
			Read = function(Buffer: buffer): (components_Target)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 1 bytes
				local BLOCK_START = Read(1)
				-- Buffer read
				local Value = {} :: any
				Value.CanTarget = (buffer.readu8(RecieveBuffer, BLOCK_START + 0) == 1)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: components_Target): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 1 bytes
				local BLOCK_START = Allocate(1)
				-- Buffer write
				buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.CanTarget and 1 or 0)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
	},
	ReplecsUpdate = {
		On = function(Listener: (Value: { Changes: any, Variants: any }) -> ()): () -> ()
			table.insert(Events.Reliable[0], Listener)
			for Index, Arguments in Queue.Reliable[0] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[0] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[0], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[0], Index)
				end
			end
		end
	},
	ReplecsUnreliableUpdate = {
		On = function(Listener: (Value: { Changes: any, Variants: any }) -> ()): () -> ()
			table.insert(Events.Unreliable[0], Listener)
			return function (): ()
				local Index = table.find(Events.Unreliable[0], Listener)
				if Index ~= nil then
					table.remove(Events.Unreliable[0], Index)
				end
			end
		end
	},
	ReplecsHandshakeRequest = {
		Fire = function(Value: number): ()
			SerdesFunctions.WriteEVENT_ReplecsHandshakeRequest(Value)
		end
	},
	ReplecsHandshakeResponse = {
		On = function(Listener: (Value: { Changes: any, Variants: any }) -> ()): () -> ()
			Events.Reliable[2] = Listener
			for Index, Arguments in Queue.Reliable[2] do
				Listener(table.unpack(Arguments))
			end
			Queue.Reliable[2] = {}
			return function (): ()
				if Events.Reliable[2] == Listener then
					Events.Reliable[2] = nil
				end
			end
		end
	},
	ActionPayload = {
		Read = function(Buffer: buffer): (ActionPayload)
			-- Buffer overwrite
			local PreviousCursor = RecieveCursor
			local PreviousBuffer = RecieveBuffer
			local PreviousInstanceCursor = RecieveInstanceCursor
			RecieveCursor = 0
			RecieveBuffer = Buffer
			-- Read BLOCK: 9 bytes
			local BLOCK_START = Read(9)
			-- Buffer read
			local Value = {} :: any
			local Length = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
			Value.actionId = buffer.readstring(RecieveBuffer, Read(Length), Length)
			if #Value.actionId < 0 then error(`Expected length of "Value.actionId" to be larger than 0, got {#Value.actionId} instead.`) end
			if #Value.actionId > 64 then error(`Expected length of "Value.actionId" to be smaller than 64, got {#Value.actionId} instead.`) end
			Length = buffer.readu8(RecieveBuffer, BLOCK_START + 1)
			Value.action = buffer.readstring(RecieveBuffer, Read(Length), Length)
			if #Value.action < 0 then error(`Expected length of "Value.action" to be larger than 0, got {#Value.action} instead.`) end
			if #Value.action > 32 then error(`Expected length of "Value.action" to be smaller than 32, got {#Value.action} instead.`) end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 2) == 1 then
				-- Read BLOCK: 12 bytes
				local BLOCK_START = Read(12)
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				Value.velocity = Vector3.new(X, Y, Z)
			end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 3) == 1 then
				-- Read BLOCK: 24 bytes
				local BLOCK_START = Read(24)
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				local Position = Vector3.new(X, Y, Z)
				local rX = buffer.readf32(RecieveBuffer, BLOCK_START + 12)
				local rY = buffer.readf32(RecieveBuffer, BLOCK_START + 16)
				local rZ = buffer.readf32(RecieveBuffer, BLOCK_START + 20)
				Value.origin = CFrame.new(Position) * CFrame.fromOrientation(rX, rY, rZ)
			end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 4) == 1 then
				-- Read BLOCK: 8 bytes
				local BLOCK_START = Read(8)
				Value.fromGun = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
			end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 5) == 1 then
				-- Read BLOCK: 8 bytes
				local BLOCK_START = Read(8)
				Value.timestamp = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
			end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 6) == 1 then
				-- Read BLOCK: 8 bytes
				local BLOCK_START = Read(8)
				Value.targetEntityId = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
			end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 7) == 1 then
				-- Read BLOCK: 8 bytes
				local BLOCK_START = Read(8)
				Value.merryGoRoundId = buffer.readf64(RecieveBuffer, BLOCK_START + 0)
			end
			if buffer.readu8(RecieveBuffer, BLOCK_START + 8) == 1 then
				-- Read BLOCK: 12 bytes
				local BLOCK_START = Read(12)
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 0)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 4)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 8)
				Value.hitPosition = Vector3.new(X, Y, Z)
			end
			-- Buffer restore
			RecieveCursor = PreviousCursor
			RecieveBuffer = PreviousBuffer
			RecieveInstanceCursor = PreviousInstanceCursor
			return Value
		end,
		Write = function(Value: ActionPayload): (buffer)
			-- Buffer overwrite
			local Previous = Save()
			Load()
			-- Allocate BLOCK: 9 bytes
			local BLOCK_START = Allocate(9)
			-- Buffer write
			local Length = #Value.actionId
			buffer.writeu8(SendBuffer, BLOCK_START + 0, Length)
			Allocate(Length)
			buffer.writestring(SendBuffer, SendOffset, Value.actionId, Length)
			local Length = #Value.action
			buffer.writeu8(SendBuffer, BLOCK_START + 1, Length)
			Allocate(Length)
			buffer.writestring(SendBuffer, SendOffset, Value.action, Length)
			buffer.writeu8(SendBuffer, BLOCK_START + 2, Value.velocity ~= nil and 1 or 0)
			if Value.velocity ~= nil then
				-- Allocate BLOCK: 12 bytes
				local BLOCK_START = Allocate(12)
				local Vector = Value.velocity
				buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
			end
			buffer.writeu8(SendBuffer, BLOCK_START + 3, Value.origin ~= nil and 1 or 0)
			if Value.origin ~= nil then
				-- Allocate BLOCK: 24 bytes
				local BLOCK_START = Allocate(24)
				local Vector = Value.origin.Position
				buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
				local rX, rY, rZ = Value.origin:ToOrientation()
				buffer.writef32(SendBuffer, BLOCK_START + 12, rX)
				buffer.writef32(SendBuffer, BLOCK_START + 16, rY)
				buffer.writef32(SendBuffer, BLOCK_START + 20, rZ)
			end
			buffer.writeu8(SendBuffer, BLOCK_START + 4, Value.fromGun ~= nil and 1 or 0)
			if Value.fromGun ~= nil then
				-- Allocate BLOCK: 8 bytes
				local BLOCK_START = Allocate(8)
				buffer.writef64(SendBuffer, BLOCK_START + 0, Value.fromGun)
			end
			buffer.writeu8(SendBuffer, BLOCK_START + 5, Value.timestamp ~= nil and 1 or 0)
			if Value.timestamp ~= nil then
				-- Allocate BLOCK: 8 bytes
				local BLOCK_START = Allocate(8)
				buffer.writef64(SendBuffer, BLOCK_START + 0, Value.timestamp)
			end
			buffer.writeu8(SendBuffer, BLOCK_START + 6, Value.targetEntityId ~= nil and 1 or 0)
			if Value.targetEntityId ~= nil then
				-- Allocate BLOCK: 8 bytes
				local BLOCK_START = Allocate(8)
				buffer.writef64(SendBuffer, BLOCK_START + 0, Value.targetEntityId)
			end
			buffer.writeu8(SendBuffer, BLOCK_START + 7, Value.merryGoRoundId ~= nil and 1 or 0)
			if Value.merryGoRoundId ~= nil then
				-- Allocate BLOCK: 8 bytes
				local BLOCK_START = Allocate(8)
				buffer.writef64(SendBuffer, BLOCK_START + 0, Value.merryGoRoundId)
			end
			buffer.writeu8(SendBuffer, BLOCK_START + 8, Value.hitPosition ~= nil and 1 or 0)
			if Value.hitPosition ~= nil then
				-- Allocate BLOCK: 12 bytes
				local BLOCK_START = Allocate(12)
				local Vector = Value.hitPosition
				buffer.writef32(SendBuffer, BLOCK_START + 0, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 4, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 8, Vector.Z)
			end
			-- Buffer restore
			local Result = buffer.create(SendCursor)
			buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
			Load(Previous)
			return Result
		end
	},
	ProcessAction = {
		Fire = function(Value: { actionId: string, action: string, velocity: Vector3?, origin: CFrame?, fromGun: number?, timestamp: number?, targetEntityId: number?, merryGoRoundId: number?, hitPosition: Vector3? }): ()
			SerdesFunctions.WriteEVENT_ProcessAction(Value)
		end
	},
	ResourcesSync = {
		On = function(Listener: (Value: { Coins: number, Gems: number, Level: number, Experience: number }) -> ()): () -> ()
			Events.Reliable[4] = Listener
			for Index, Arguments in Queue.Reliable[4] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[4] = {}
			return function (): ()
				if Events.Reliable[4] == Listener then
					Events.Reliable[4] = nil
				end
			end
		end
	},
	SettingsSync = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			Events.Reliable[5] = Listener
			for Index, Arguments in Queue.Reliable[5] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[5] = {}
			return function (): ()
				if Events.Reliable[5] == Listener then
					Events.Reliable[5] = nil
				end
			end
		end
	},
	StatisticsSync = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			Events.Reliable[6] = Listener
			for Index, Arguments in Queue.Reliable[6] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[6] = {}
			return function (): ()
				if Events.Reliable[6] == Listener then
					Events.Reliable[6] = nil
				end
			end
		end
	},
	PlayerlistSync = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			table.insert(Events.Reliable[7], Listener)
			for Index, Arguments in Queue.Reliable[7] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[7] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[7], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[7], Index)
				end
			end
		end
	},
	InventorySync = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			Events.Reliable[8] = Listener
			for Index, Arguments in Queue.Reliable[8] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[8] = {}
			return function (): ()
				if Events.Reliable[8] == Listener then
					Events.Reliable[8] = nil
				end
			end
		end
	},
	RewardsSync = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			Events.Reliable[9] = Listener
			for Index, Arguments in Queue.Reliable[9] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[9] = {}
			return function (): ()
				if Events.Reliable[9] == Listener then
					Events.Reliable[9] = nil
				end
			end
		end
	},
	TradingActiveTradeSync = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			Events.Reliable[10] = Listener
			for Index, Arguments in Queue.Reliable[10] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[10] = {}
			return function (): ()
				if Events.Reliable[10] == Listener then
					Events.Reliable[10] = nil
				end
			end
		end
	},
	VotingSync = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			table.insert(Events.Reliable[11], Listener)
			for Index, Arguments in Queue.Reliable[11] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[11] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[11], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[11], Index)
				end
			end
		end
	},
	RoundStatusSync = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[12], Listener)
			for Index, Arguments in Queue.Reliable[12] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[12] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[12], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[12], Index)
				end
			end
		end
	},
	InventoryItemAdded = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			table.insert(Events.Reliable[13], Listener)
			for Index, Arguments in Queue.Reliable[13] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[13] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[13], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[13], Index)
				end
			end
		end
	},
	InventoryItemRemoved = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			table.insert(Events.Reliable[14], Listener)
			for Index, Arguments in Queue.Reliable[14] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[14] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[14], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[14], Index)
				end
			end
		end
	},
	InventoryEquipItem = {
		Invoke = function(Value: string): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_InventoryEquipItem(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"InventoryEquipItem\".")
			end
			return ReturnValue
		end
	},
	InventoryUnequipItem = {
		Invoke = function(Value: string): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_InventoryUnequipItem(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"InventoryUnequipItem\".")
			end
			return ReturnValue
		end
	},
	InventoryLockItem = {
		Invoke = function(Value: string): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_InventoryLockItem(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"InventoryLockItem\".")
			end
			return ReturnValue
		end
	},
	InventoryUnlockItem = {
		Invoke = function(Value: string): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_InventoryUnlockItem(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"InventoryUnlockItem\".")
			end
			return ReturnValue
		end
	},
	InventoryToggleFavorite = {
		Invoke = function(Value1: string, Value2: boolean): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_InventoryToggleFavorite(Value1, Value2, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"InventoryToggleFavorite\".")
			end
			return ReturnValue
		end
	},
	InventoryOpenCrate = {
		Invoke = function(Value: string): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_InventoryOpenCrate(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"InventoryOpenCrate\".")
			end
			return ReturnValue
		end
	},
	RewardsClaimDailyReward = {
		Invoke = function(Value: nil): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_RewardsClaimDailyReward(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"RewardsClaimDailyReward\".")
			end
			return ReturnValue
		end
	},
	AchievementsChanged = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			table.insert(Events.Reliable[22], Listener)
			for Index, Arguments in Queue.Reliable[22] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[22] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[22], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[22], Index)
				end
			end
		end
	},
	AchievementsClaimAchievement = {
		Invoke = function(Value: string): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_AchievementsClaimAchievement(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"AchievementsClaimAchievement\".")
			end
			return ReturnValue
		end
	},
	AchievementsGetAchievements = {
		Invoke = function(Value: nil): (any)
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_AchievementsGetAchievements(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: any = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"AchievementsGetAchievements\".")
			end
			return ReturnValue
		end
	},
	RoundStartMatch = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			table.insert(Events.Reliable[25], Listener)
			for Index, Arguments in Queue.Reliable[25] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[25] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[25], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[25], Index)
				end
			end
		end
	},
	RoundEndMatch = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			table.insert(Events.Reliable[26], Listener)
			for Index, Arguments in Queue.Reliable[26] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[26] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[26], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[26], Index)
				end
			end
		end
	},
	RoundApplyTeamIndicator = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			Events.Reliable[27] = Listener
			for Index, Arguments in Queue.Reliable[27] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[27] = {}
			return function (): ()
				if Events.Reliable[27] == Listener then
					Events.Reliable[27] = nil
				end
			end
		end
	},
	RoundSendDistraction = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			table.insert(Events.Reliable[28], Listener)
			for Index, Arguments in Queue.Reliable[28] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[28] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[28], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[28], Index)
				end
			end
		end
	},
	VoteSubmit = {
		Fire = function(Value: { field: string, choice: string }): ()
			SerdesFunctions.WriteEVENT_VoteSubmit(Value)
		end
	},
	NotificationAdd = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			table.insert(Events.Reliable[30], Listener)
			for Index, Arguments in Queue.Reliable[30] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[30] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[30], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[30], Index)
				end
			end
		end
	},
	NotificationRemove = {
		On = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[31], Listener)
			for Index, Arguments in Queue.Reliable[31] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[31] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[31], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[31], Index)
				end
			end
		end
	},
	ShopSetGiftPlayer = {
		Fire = function(Value: Player?): ()
			SerdesFunctions.WriteEVENT_ShopSetGiftPlayer(Value)
		end
	},
	ShopSubmitCode = {
		Invoke = function(Value: string): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_ShopSubmitCode(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"ShopSubmitCode\".")
			end
			return ReturnValue
		end
	},
	ShopPurchaseCrate = {
		Invoke = function(Value: { crateType: string, quantity: number }): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_ShopPurchaseCrate(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"ShopPurchaseCrate\".")
			end
			return ReturnValue
		end
	},
	ShopGetGiftedGamepasses = {
		Invoke = function(Value: Player): (any)
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_ShopGetGiftedGamepasses(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: any = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"ShopGetGiftedGamepasses\".")
			end
			return ReturnValue
		end
	},
	SettingsChangeSetting = {
		Invoke = function(Value1: string, Value2: any): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_SettingsChangeSetting(Value1, Value2, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"SettingsChangeSetting\".")
			end
			return ReturnValue
		end
	},
	TradingTradeReceived = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			table.insert(Events.Reliable[37], Listener)
			for Index, Arguments in Queue.Reliable[37] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[37] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[37], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[37], Index)
				end
			end
		end
	},
	TradingTradeCompleted = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			table.insert(Events.Reliable[38], Listener)
			for Index, Arguments in Queue.Reliable[38] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[38] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[38], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[38], Index)
				end
			end
		end
	},
	TradingTradeProcessed = {
		On = function(Listener: (Value: any) -> ()): () -> ()
			table.insert(Events.Reliable[39], Listener)
			for Index, Arguments in Queue.Reliable[39] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[39] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[39], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[39], Index)
				end
			end
		end
	},
	TradingSendTradeToPlayer = {
		Invoke = function(Value: Player): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_TradingSendTradeToPlayer(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"TradingSendTradeToPlayer\".")
			end
			return ReturnValue
		end
	},
	TradingAcceptTradeRequest = {
		Invoke = function(Value: string): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_TradingAcceptTradeRequest(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"TradingAcceptTradeRequest\".")
			end
			return ReturnValue
		end
	},
	TradingDeclineTradeRequest = {
		Invoke = function(Value: string): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_TradingDeclineTradeRequest(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"TradingDeclineTradeRequest\".")
			end
			return ReturnValue
		end
	},
	TradingAddItem = {
		Invoke = function(Value: { tradeUuid: string, itemUuid: string }): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_TradingAddItem(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"TradingAddItem\".")
			end
			return ReturnValue
		end
	},
	TradingRemoveItem = {
		Invoke = function(Value: { tradeUuid: string, itemUuid: string }): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_TradingRemoveItem(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"TradingRemoveItem\".")
			end
			return ReturnValue
		end
	},
	TradingAcceptTrade = {
		Invoke = function(Value: string): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_TradingAcceptTrade(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"TradingAcceptTrade\".")
			end
			return ReturnValue
		end
	},
	TradingDeclineTrade = {
		Invoke = function(Value: string): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_TradingDeclineTrade(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"TradingDeclineTrade\".")
			end
			return ReturnValue
		end
	},
	TradingConfirmTrade = {
		Invoke = function(Value: string): ({ Success: boolean, Message: string?, Response: any? })
			local InvocationIdentifier = Invoke()
			if Calls[InvocationIdentifier] ~= nil then
				Invocations -= 1
				error("More than 256 calls are awaiting a response, this packet has been dropped.")
			end
			SerdesFunctions.WriteFUNCTION_DATA_TradingConfirmTrade(Value, InvocationIdentifier)
			Calls[InvocationIdentifier] = coroutine.running()
			local Success, ReturnValue: { Success: boolean, Message: string?, Response: any? } = coroutine.yield()
			if Success ~= true then
				error("There was an exception while processing \"TradingConfirmTrade\".")
			end
			return ReturnValue
		end
	},
	VoxelDestroyCells = {
		On = function(Listener: (Value: { entityId: number, indices: buffer, hitPosition: Vector3, explosionForce: number, debrisLifetime: number }) -> ()): () -> ()
			table.insert(Events.Reliable[48], Listener)
			for Index, Arguments in Queue.Reliable[48] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[48] = {}
			return function (): ()
				local Index = table.find(Events.Reliable[48], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[48], Index)
				end
			end
		end
	},
})
type BLINK_EVENTS_SYMBOL = typeof(Returns)
return Returns :: BLINK_EVENTS_SYMBOL