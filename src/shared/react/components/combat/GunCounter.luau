--!strict

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local React = require(ReplicatedStorage.packages.React)

local function GunCounter(_props) end

return React.memo(GunCounter)
